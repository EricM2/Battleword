<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.battleword">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.app.battle_word.LoadWordsActivity"></activity>
        <activity android:name="com.app.battle_word.LeaveGameActivity" />

        <service
            android:name="com.app.battle_word.BackgroundSoundService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".SettingsActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".NextStageActivity" />
        <activity android:name=".GameOverActivity" />
        <activity android:name=".NewGameActivity" />
        <activity
            android:name=".GameScenarioActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".PlayerControlActivity" />
        <activity android:name=".CountDownActivity" />
        <activity android:name=".GameSetupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>